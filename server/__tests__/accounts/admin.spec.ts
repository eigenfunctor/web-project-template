import * as request from "supertest";

describe("admin > set admin, list users", () => {
  it("should list all users to admins.", () => {});
  it("should not list all users to a non-admin.", () => {});
  it("should set a user to admin by another admin.", () => {});
  it("should not set a user to admin by a non-admin.", () => {});
  it("should create a root account with a root password", () => {});
  it("should verify root login using root password", () => {});
  it("should disallow  root login if ENABLE_ROOT_ACCOUNT is unset", () => {});
  it("should not allow demotion of the root account.", () => {});
});
